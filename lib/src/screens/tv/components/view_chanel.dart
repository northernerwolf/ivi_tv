import 'package:flutter/material.dart';

class ViewChanel extends StatelessWidget {
  const ViewChanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
